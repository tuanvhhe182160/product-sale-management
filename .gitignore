# --- NetBeans Specifics ---
# File cấu hình riêng tư của từng máy (vị trí cửa sổ, file đang mở...)
/nbproject/private/

# Thư mục chứa code đã biên dịch (file .class) -> NetBeans tự build lại
/build/
/web/WEB-INF/classes/

# Thư mục chứa file đóng gói (.war)
/dist/

# --- OS Specifics (File rác hệ điều hành) ---
.DS_Store
Thumbs.db

# --- Secrets / Configuration (QUAN TRỌNG) ---
# Bỏ qua file chứa mật khẩu thật để tránh lộ thông tin lên Git
src/java/com/techshop/conf/db.properties

# --- Logs ---
*.log